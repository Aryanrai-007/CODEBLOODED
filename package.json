{
  "name": "codex-lethalis-app",
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "cd src/frontend && npm run build",
    "typecheck": "cd src/frontend && npm run typecheck",
    "check": "cd src/frontend && npm run check",
    "fix": "cd src/frontend && npm run fix"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  }
}
